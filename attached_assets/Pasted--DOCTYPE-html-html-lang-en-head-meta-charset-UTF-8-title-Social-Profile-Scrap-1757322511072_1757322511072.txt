<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Social Profile Scraper</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    body, html {
      height: 100%;
      background: linear-gradient(135deg, #667eea, #764ba2);
      color: white;
      margin: 0;
      padding: 0;
      font-family: Arial, sans-serif;
    }
    .centered-form {
      height: 100%;
      display: flex;
      justify-content: center;
      align-items: center;
    }
    .form-container {
      background: rgba(0, 0, 0, 0.6);
      padding: 2.5rem;
      border-radius: 15px;
      box-shadow: 0 8px 24px rgba(0,0,0,0.3);
      width: 100%;
      max-width: 500px;
    }
    h2 {
      text-align: center;
      margin-bottom: 1.5rem;
      font-weight: bold;
    }
    label {
      font-weight: 500;
    }
    input.form-control {
      background: rgba(255,255,255,0.1);
      border: none;
      color: white;
    }
    input.form-control::placeholder {
      color: #ccc;
    }
    button.btn-primary {
      background-color: #764ba2;
      border: none;
      width: 100%;
      margin-top: 1rem;
      transition: background-color 0.3s ease;
    }
    button.btn-primary:hover {
      background-color: #667eea;
    }
    .alert {
      margin-top: 1rem;
    }
  </style>
</head>
<body>
  <div class="centered-form">
    <form class="form-container" method="post" action="/">
      <h2>Get TikTok Profile Info</h2>

      {% if error %}
      <div class="alert alert-danger" role="alert">
        {{ error }}
      </div>
      {% endif %}

      <div class="mb-3">
        <label for="full_name" class="form-label">Full Name</label>
        <input
          type="text"
          id="full_name"
          name="full_name"
          placeholder="e.g. Cristiano Ronaldo"
          class="form-control"
          value="{{ request.form.full_name or '' }}"
          required
        />
      </div>

      <div class="mb-3">
        <label for="tiktok_url" class="form-label">TikTok URL</label>
        <input
          type="url"
          id="tiktok_url"
          name="tiktok_url"
          placeholder="https://www.tiktok.com/@c.ronaldoxd7"
          class="form-control"
          value="{{ request.form.tiktok_url or '' }}"
          required
        />
      </div>

      <button type="submit" class="btn btn-primary">Submit</button>
    </form>
  </div>
</body>
</html>
